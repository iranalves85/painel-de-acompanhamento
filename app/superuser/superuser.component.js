angular.
module('superuser').
component('superuser', {
    // Note: The URL is relative to our `index.html` file
    templateUrl: 'app/superuser/superuser.template.html',
    controller: function() {}
});